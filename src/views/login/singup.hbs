<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-header">
                    <h3>Registro</h3>
                </div>
                <div class="card-body">
                {{#if id }}
                    <form action="/registro/{{id}}" method="POST">
                {{else}}
                    <form action="/registro" method="POST">
                {{/if}}
                    <div class="form-group">
                        <input type="text" maxlength="50" name="username" placeholder="Su e-mail" class="form-control" autofocus>
                    </div>
                    <div class="form-group">
                        <input type="text" maxlength="50" name="name" placeholder="Su nombre y apellidos" class="form-control" autofocus>
                    </div>
                    <div class="form-group">
                        <input type="password" maxlength="50" name="pass" placeholder="Contraseña" class="form-control">
                    </div>
                    <div class="form-group">
                        <button class="btn btn-success btn-block">Registrarse</button>
                    </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
