<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-header">
                    <h3>Inicio de sesión</h3>
                </div>
                <div class="card-body">
                    {{#if id }}
                        <form action="/inicio/{{id}}" method="POST">
                    {{else}}
                        <form action="/inicio" method="POST">
                    {{/if}} 
                        <div class="form-group">
                        <input type="text" maxlength="50" name="username" placeholder="Su correo" class="form-control" autofocus>
                    </div>
                    <div class="form-group">
                        <input type="password" maxlength="50" name="pass" placeholder="Contraseña" class="form-control">
                    </div>
                    <div class="form-group">
                        <button class="btn btn-success btn-block">Iniciar sesión</button>
                    </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    {{#if id }}
    <a href="/registro/{{id}}">¿Nuevo usuario? Registrese aquí, se le inscribira automaticamente en el seminario</a>
    {{else}}
    <a href="/registro">¿Nuevo usuario? Registrese aquí</a>
    {{/if}}
</div>